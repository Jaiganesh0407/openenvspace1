[project]
name = "openenv-agent"
version = "0.1.0"
description = "OpenEnv AI Agent"
requires-python = ">=3.9"

dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "gradio",
  "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"
